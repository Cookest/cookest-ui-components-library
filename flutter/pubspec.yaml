name: cookest_ui
description: Cookest design system — Flutter widget library with shared cross-platform tokens.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.7
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
